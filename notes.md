# disk size
* du -h -d 1 . | sort -h

# nvim
<x>gg in visual to jump to line

# database
* mandatory use of SQL/xml/json is required
* https://github.com/leethomason/tinyxml2

# add a .clangd file to solve include issues

# common main.cpp. serverMain. clientMain. can switch with argument
